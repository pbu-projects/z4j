micronaut:
  application:
    name: zendesk4j
  http:
    services:
      zendesk:
        url: # MICRONAUT_HTTP_SERVICES_ZENDESK_URL
        email: # MICRONAUT_HTTP_SERVICES_ZENDESK_EMAIL
        token: # MICRONAUT_HTTP_SERVICES_ZENDESK_TOKEN